<%= render 'users/list', user: current_user, book: @book %>
<div class="col-xs-9">
  <table class="table">
  	<thead>
    		<h2>Users</h2>
    		<tr>
         		<th>image</th>
         		<th>name</th>
         		<th></th>
      	</tr>
    	</thead>
    	<tbody>
    	　<% @users.each do |user| %>
    		<tr>
    			<td>
    				<%= attachment_image_tag user, :profile_image, :fill, 100, 100, format: 'jpeg', size: '40x40', fallback: "no_image.jpg" %>
    			</td>
    	  		<td><%= user.name %></td>
    	  		<td>
    	  			<%= link_to "show", user_path(user) %>
    	  		</td>
    		</tr>
    	   <% end %>
    	</tbody>
  </table>
</div>

