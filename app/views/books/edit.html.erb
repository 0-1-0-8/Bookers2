<div class="top-message col_md-offset-3 col-md-3">
	<h1>EditingBook</h1>

	<%= form_for(@book) do |f| %>
		<% if @book.errors.any? %>
		        <h2><%= @book.errors.count %>errors prohibited this obj from being saved:</h2>
		        <% @book.errors.full_messages.each do |message| %>
		            <li><%= message %></li>
		        <% end %>
		<% end %>


		  <h4>Title</h4>
		  <%= f.text_field :title %>
		  <h4>Opinion</h4>
		  <%= f.text_field :body %>
		  <%= f.submit 'UpdateBook', class:"btn btn-primary col-xs-12" %>
	<% end %>

		<div>
		  <%= link_to "show", book_path(@book) %>
		  <%= link_to "Back", books_path %>
		</div>
</div>